# Deploy — 2026-06-16 round

Living tracker for the current batch of changes to upload to the server.
**Append to this as more changes land** before the upload. The headline item is
the **grouped Publications ("folders")** feature, which needs a DB migration.

---

## 1. Run this SQL FIRST

```
deploy/migration_2026_06_16.sql
```

Run both against the production `eswasa` database before uploading the PHP, so
the new tables/columns exist when the new code runs. Both are idempotent
(safe to re-run):

```
deploy/migration_2026_06_16.sql           # Publications grouped folders
deploy/migration_2026_06_16_tenders.sql   # Tenders section
```

`migration_2026_06_16.sql`:
- Creates table `eswasa_publication_groups` and seeds the 5 system type groups
  (Annual Reports, Reports, Guidance Documents, Newsletters, Standards).
- Adds column `eswasa_publications.group_id` (nullable).

`migration_2026_06_16_tenders.sql`:
- Creates tables `eswasa_tenders` and `eswasa_tender_documents`.

Also run this small patch (re-points the Certification Journey picture to the
edited graphic):
```
deploy/patch_cert_steps_2026_06_16.sql
```

Quick check after running:
```sql
SELECT id,name,type_key,sort_order,is_system FROM eswasa_publication_groups ORDER BY sort_order;
SHOW COLUMNS FROM eswasa_publications LIKE 'group_id';
```

---

## 2. Upload these files (preserve folder structure)

Paths are relative to web root (where `index.php` lives).

### New files
```
deploy/migration_2026_06_16.sql
deploy/migration_2026_06_16_tenders.sql

tenders.php
includes/cms_keys_tenders.php
admin/pages/tenders_edit.php
admin/pages/_tender_fields.php
```
> Also ensure the directory `admin/uploads/tenders/` exists and is writable
> (the app auto-creates it on first upload, but create it if your host blocks
> runtime `mkdir`).

### Changed files — Publications grouped folders
```
publications.php
admin/pages/publications_edit.php
```

### Changed files — Tenders wiring
```
includes/header.php             # Updates dropdown gains "Tenders"
admin/index.php                 # registers tenders_edit.php (allowed pages + title)
admin/includes/sidebar.php      # Tenders link in the Updates group
```

### Changed files — also in this round (code-only, no SQL)
```
Meetourteam.php                 # staff group photo now hidden by default + admin toggle
admin/pages/about_team.php      # "Show staff group photo" switch
includes/header.php             # WhatsApp link + Tenders menu item
includes/footer.php             # WhatsApp link -> https://wa.me/26878780415
Certification.php               # Journey picture repointed to edited SVG (Gap Analysis + Internal Audit removed)
assets/img/steps-to-certification.svg   # edited infographic — now 3 steps
index.php                       # SWASA -> ESWASA (Facebook embed fallback text)
training-calendar.php           # SWASA -> ESWASA (title + meta)
training-about.php              # SWASA -> ESWASA (meta description)
```

> **Manual fix still outstanding:** certification document #5's file is named
> `Impartiality Policy - SWASA Certification.pdf` (a manual path in
> `certification_documents`, stored on the server). Rename that file on the
> server to `...ESWASA...` and update its path in **Admin ▸ Management Systems ▸
> Certification Documents** (the visible title "Impartiality Policy" is already
> clean). Not fixable from local — the file isn't in the repo.

> Repo-only (do NOT need to be on the server): `docs/superpowers/specs/2026-06-16-grouped-publications-design.md`.

---

## 3. Post-deploy checks

1. **Publications page** (`/publications.php`): with no documents, shows the
   empty-state message. Add documents in admin and confirm each appears under
   its folder, and folders with no documents stay hidden.
2. **Admin ▸ Publications ▸ Folders tab**: reorder/rename works; add a custom
   folder; assign a document to it (Documents tab → Folder dropdown); delete the
   custom folder and confirm its documents return to Auto (by type).
3. **Meet Our Team**: staff group photo hidden until the admin switch is ticked.
4. **WhatsApp** icons (header + footer) open `wa.me/26878780415`.
5. **Tenders** (`/tenders.php`, also in *Updates ▸ Tenders* menu): add a tender
   in admin with a future closing date + a couple of PDFs → appears under **Open
   Tenders** with its documents. Add one with a past closing date → appears in
   the **Closed Tenders** archive. Delete a tender → its documents/files go too.

> Note: `includes/header.php` carries both the WhatsApp and the Tenders-menu
> change — upload it once.

---

## 4. Notes / housekeeping

- Working tree also shows `admin/uploads/publications/CALIBRATION TAB.pdf` as
  **deleted**. This was not part of the feature work — decide separately whether
  to remove it on the server (it may be referenced by an old publication row).
- Prior cropper commits (`29d9f1b`…`dca9bdb`) predate this round; if they were
  not deployed in an earlier upload, bundle those files too.
